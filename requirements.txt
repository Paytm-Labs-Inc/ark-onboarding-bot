html2text>=2024.2.26
python-dotenv
numpy>=1.26
sentence-transformers>=3.0,<4.0
cursor-sdk>=0.1
fastapi>=0.115
uvicorn>=0.32
httpx>=0.27
# Answer generation uses Pi Inference (PI_API_KEY) by default; the Cursor agent
# CLI + CURSOR_API_KEY is the optional ANSWER_BACKEND=cursor path.
slack-bolt>=1.18
