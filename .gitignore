.venv/
__pycache__/
*.pyc
.env
.chroma/
*.sqlite3
data/*.embeddings
sources/faq-google-doc.*
eval/feedback.jsonl
eval/query_log.jsonl
eval/triage-reports/
eval/results/

# Benchmark output: large, timestamped, and not reproducible run-to-run
# (no temperature pinned, +/-7/50 noise band). Committing it puts tens of
# thousands of lines of unreviewable JSON in front of anyone reviewing a
# deploy. Keep runs local; publish conclusions, not raw judge output.
eval/bench/results/

# Local scratch: smoke-test output and worktree pointers, committed by accident.
.smoke_session_results.txt
.worktrees/
